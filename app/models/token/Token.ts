export interface Token {
  accessToken: string; // JWT token
  expiration: string;
  refreshToken: string;
}

// export interface Token {
//   accessToken: string; // JWT token
//   expiration: Date;
//   refreshToken: string;
// }
