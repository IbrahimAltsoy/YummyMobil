export interface GoogleNearPlaces {
  name: string; // İşletme adı
  vicinity: string; // İşletme adresi
  rating: number; // İşletme puanı
  place_id: string;
  photoUrl: string;
  user_Ratings_Total: number;
  distance: number;
}
